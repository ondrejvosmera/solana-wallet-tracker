import { ShyftConfig } from '../utils';
import { WalletKeypair } from '../types';
export declare class SemiCustodialWalletClient {
    private readonly config;
    constructor(config: ShyftConfig);
    create(input: {
        password: string;
    }): Promise<string>;
    getKeypair(input: {
        password: string;
        walletAddress: string;
    }): Promise<WalletKeypair>;
    changePassword(input: {
        currentPassword: string;
        newPassword: string;
        walletAddress: string;
    }): Promise<boolean>;
}

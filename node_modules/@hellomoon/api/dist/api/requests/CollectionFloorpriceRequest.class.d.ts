import { BaseRequest } from "../BaseRequest.class";
import type { PaginatedResponse } from "../Response.interface";
export interface CollectionFloorprice {
    /**
     * > To find the correct helloMoonCollectionId, **[click here](https://www.hellomoon.io/id?search=collection)**
     * and search a collection name. This list is continuously updated.
     * `optional field`
     */
    helloMoonCollectionId: string;
    /**
     * The floor price of the collection in Lamports
     */
    floorPriceLamports: string;
    /**
     * The current volume (the sum of nft sales) of the collection in the last minute. If no sales have occurred
     * in the last minute, this value will be 0.
     */
    volumeLamports: string;
    /**
     * Epoch time in seconds. You can adjust the startTime to look at Historical Stats and Floor Prices.
     *     For example, startTime < 1622576000 (June 1, 2021) will return NFT Collection Stats before 1622576000.
     *
     * If you want to look at historical data, let's say 7 days in the past.
     * > 1. _Change the **operator** to <_
     * > 2. Get the **current epochtime** i.e, 1673831466 -> Jan 15, 2023
     * > 3. Subtract the current epochtime from **( 86400 * 7 )**. Place the result of 1673831466 - ( 86400
     * * 7 ) = **1673226666** in the value input - this returns the **startTime** time from 7 days ago
     */
    startTime: number;
    /**
     * The number of listings for the collection
     */
    listing_count: number;
    /**
     * The time granularity (period): ONE_MIN, FIVE_MIN, ONE_HOUR, ONE_DAY, ONE_WEEK. For example, the ONE_MIN
     * granularity will return a candlestick for every minute in the time period - as long as there is volume.
     * `optional field`
     */
    granularity: "ONE_MIN" | "FIVE_MIN" | "ONE_HOUR" | "ONE_DAY" | "ONE_WEEK";
}
export interface CollectionFloorpriceRequestArgs {
    /**
     * > To find the correct helloMoonCollectionId, **[click here](https://www.hellomoon.io/id?search=collection)**
     * and search a collection name. This list is continuously updated.
     * `optional field`
     */
    helloMoonCollectionId?: string;
    /**
     * The time granularity (period): ONE_MIN, FIVE_MIN, ONE_HOUR, ONE_DAY, ONE_WEEK. For example, the ONE_MIN
     * granularity will return a candlestick for every minute in the time period - as long as there is volume.
     * `optional field`
     */
    granularity?: "ONE_MIN" | "FIVE_MIN" | "ONE_HOUR" | "ONE_DAY" | "ONE_WEEK";
    /**
     * The number of results to return per page
     * `optional field`
     */
    limit?: number;
    /**
     * The page number to return
     * `optional field`
     */
    page?: number;
    /**
     * The pagination token to use to keep your position in the results
     * `optional field`
     */
    paginationToken?: string;
}
export declare class CollectionFloorpriceRequest extends BaseRequest<CollectionFloorpriceRequestArgs, PaginatedResponse<CollectionFloorprice>> {
    readonly body: CollectionFloorpriceRequestArgs;
    readonly method = "POST";
    readonly url = "/v0/nft/collection/floorprice";
    constructor(body?: CollectionFloorpriceRequestArgs);
}

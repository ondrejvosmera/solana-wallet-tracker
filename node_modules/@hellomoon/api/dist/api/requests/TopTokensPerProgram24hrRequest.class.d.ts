import { BaseRequest } from "../BaseRequest.class";
import type { PaginatedResponse } from "../Response.interface";
export interface TopTokensPerProgram24hr {
    /**
     * The public key (address) of the account containing the program on chain.
     *       > You can also visit https://www.hellomoon.io/id?search=program to search for a program using a
     * user interface.
     */
    programId: string;
    /**
     * Mint address per the SPL token program.
     *       > You can also visit https://www.hellomoon.io/id?search=token to search for mints using a user
     * interface.
     */
    mint: string;
    /**
     * The amount of token volume accumulated over the last 24 hours
     */
    vol24hr: number;
    /**
     * The amount the token volume has changed over the last 24 hours
     */
    change24hr: number;
}
export interface TopTokensPerProgram24hrRequestArgs {
    /**
     * The public key (address) of the account containing the program on chain.
     *       > You can also visit https://www.hellomoon.io/id?search=program to search for a program using a
     * user interface.
     */
    programId?: string;
    /**
     * The number of results to return per page
     * `optional field`
     */
    limit?: number;
    /**
     * The page number to return
     * `optional field`
     */
    page?: number;
    /**
     * The pagination token to use to keep your position in the results
     * `optional field`
     */
    paginationToken?: string;
}
export declare class TopTokensPerProgram24hrRequest extends BaseRequest<TopTokensPerProgram24hrRequestArgs, PaginatedResponse<TopTokensPerProgram24hr>> {
    readonly body: TopTokensPerProgram24hrRequestArgs;
    readonly method = "POST";
    readonly url = "/v0/defi/program-stats/top-tokens";
    constructor(body?: TopTokensPerProgram24hrRequestArgs);
}

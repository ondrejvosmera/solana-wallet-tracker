import { BaseRequest } from "../BaseRequest.class";
import type { PaginatedResponse } from "../Response.interface";
export interface DefiProgramOverlap {
    /**
     * The public key (address) of the account containing the program on chain.
     *       > You can also visit https://www.hellomoon.io/id?search=program to search for a program using a
     * user interface.
     */
    aProgramId: string;
    /**
     * The public key (address) of the account containing the program on chain.
     *       > You can also visit https://www.hellomoon.io/id?search=program to search for a program using a
     * user interface.
     */
    bProgramId: string;
    /**
     * Number of wallets that own both A and B
     */
    numOverlap: number;
    /**
     * Total number of wallets that own A
     */
    numTotal: number;
    /**
     * Percentage of all owners of A that also own B
     */
    percentOverlap: number;
}
export interface DefiProgramOverlapRequestArgs {
    /**
     * The public key (address) of the account containing the program on chain.
     *       > You can also visit https://www.hellomoon.io/id?search=program to search for a program using a
     * user interface.
     */
    aProgramId?: string;
    /**
     * The public key (address) of the account containing the program on chain.
     *       > You can also visit https://www.hellomoon.io/id?search=program to search for a program using a
     * user interface.
     */
    bProgramId?: string;
    /**
     * The number of results to return per page
     * `optional field`
     */
    limit?: number;
    /**
     * The page number to return
     * `optional field`
     */
    page?: number;
    /**
     * The pagination token to use to keep your position in the results
     * `optional field`
     */
    paginationToken?: string;
}
export declare class DefiProgramOverlapRequest extends BaseRequest<DefiProgramOverlapRequestArgs, PaginatedResponse<DefiProgramOverlap>> {
    readonly body: DefiProgramOverlapRequestArgs;
    readonly method = "POST";
    readonly url = "/v0/defi/program-stats/overlap";
    constructor(body?: DefiProgramOverlapRequestArgs);
}

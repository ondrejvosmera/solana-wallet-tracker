import { BaseRequest } from "../BaseRequest.class";
import type { PaginatedResponse } from "../Response.interface";
export interface JupiterPairsBrokenDownWeekly {
    /**
     * The category of the swap pair.
     *         Jupiter only: The swap pair stats on Jupiter.
     *         Whole market: The swap pair stats on Jupiter + swaps on other AMMs.
     *         Per AMM: The swap pair stats broken down by AMM.
     */
    category: "Jupiter only" | "Whole market" | "Per AMM";
    /**
     * If category = 'Per AMM', then this contains the programId of the AMM, otherwise it's NULL
     * `optional field`
     */
    subCategory: string;
    /**
     * The week where volume is being recorded for the swap pair.
     */
    week: string;
    /**
     * The swap pair. In the format of tokenA - tokenB where tokenA's symbol is alphabetically before tokenB.
     * `optional field`
     */
    swapPair: string;
    /**
     * The usd volume of the swap pair over the week.
     */
    usdVolume: number;
}
export interface JupiterPairsBrokenDownWeeklyRequestArgs {
    category?: "Jupiter only" | "Whole market" | "Per AMM" | Array<"Jupiter only" | "Whole market" | "Per AMM">;
    /**
     * If category = 'Per AMM', then this contains the programId of the AMM, otherwise it's NULL
     * `optional field`
     */
    subCategory?: string;
    /**
     * The swap pair. In the format of tokenA - tokenB where tokenA's symbol is alphabetically before tokenB.
     * `optional field`
     */
    swapPair?: string;
    /**
     * The number of results to return per page
     * `optional field`
     */
    limit?: number;
    /**
     * The page number to return
     * `optional field`
     */
    page?: number;
    /**
     * The pagination token to use to keep your position in the results
     * `optional field`
     */
    paginationToken?: string;
}
export declare class JupiterPairsBrokenDownWeeklyRequest extends BaseRequest<JupiterPairsBrokenDownWeeklyRequestArgs, PaginatedResponse<JupiterPairsBrokenDownWeekly>> {
    readonly body: JupiterPairsBrokenDownWeeklyRequestArgs;
    readonly method = "POST";
    readonly url = "/v0/defi/swaps/weekly-pairs";
    constructor(body?: JupiterPairsBrokenDownWeeklyRequestArgs);
}

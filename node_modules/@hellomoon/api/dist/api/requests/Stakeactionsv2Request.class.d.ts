import { BaseRequest } from "../BaseRequest.class";
import type { PaginatedResponse } from "../Response.interface";
export interface Stakeactionsv2 {
    /**
     * Numeric identifier of a block describing the slot that the block was produced in
     */
    blockId: number;
    /**
     * HelloMoon unique identifier for specific instruction within a transaction
     */
    instructionId: string;
    /**
     * First signature in a transaction, which can be used to track and verify the transaction status across
     * the complete ledger.
     *         It is a base-58 encoded string that is uniquely generated for each transaction.
     * `optional field`
     */
    transactionId: string;
    /**
     * Unix epoch time (in seconds) of a block as calculated from validator votes.
     *     If you want to look at historical data, let's say 7 days in the past.
     *     1. _Change the **operator** to <_
     *     2. Get the **current epochtime** i.e, 1673831466 -> Jan 15, 2023
     *     3. Subtract the current epochtime from **( 86400 * 7 )**. Place the result of 1673831466 - ( 86400
     * * 7 ) = **1673226666** in the value input - this returns the epochtime time from 7 days ago
     */
    blockTime: number;
    /**
     * true if staking is activated, false if it is deactivated
     * `optional field`
     */
    activate: boolean;
    /**
     * The public key of the stake account
     * `optional field`
     */
    stakeAccount: string;
    /**
     * When someone wants to delegate tokens in a stake account, the delegation command is pointed at the vote
     * account address of the validator to whom the token-holder wants to delegate.
     * `optional field`
     */
    voteAccount: string;
    /**
     * account owner (public key), given authority to stake tokens
     * `optional field`
     */
    stakeAuthority: string;
    /**
     * balance (in lamports) of the stake account being acivated or deactivated
     */
    balance: number;
}
export interface Stakeactionsv2RequestArgs {
    blockTime?: {
        operator: "=" | "!=" | ">" | "<" | ">=" | "<=";
        /**
     * Unix epoch time (in seconds) of a block as calculated from validator votes.
     *     If you want to look at historical data, let's say 7 days in the past.
     *     1. _Change the **operator** to <_
     *     2. Get the **current epochtime** i.e, 1673831466 -> Jan 15, 2023
     *     3. Subtract the current epochtime from **( 86400 * 7 )**. Place the result of 1673831466 - ( 86400
     * * 7 ) = **1673226666** in the value input - this returns the epochtime time from 7 days ago
     */
        value: number;
    } | number;
    /**
     * true if staking is activated, false if it is deactivated
     * `optional field`
     */
    activate?: boolean;
    balance?: {
        operator: "=" | "!=" | ">" | "<" | ">=" | "<=";
        /**
     * balance (in lamports) of the stake account being acivated or deactivated
     */
        value: number;
    } | number;
    /**
     * First signature in a transaction, which can be used to track and verify the transaction status across
     * the complete ledger.
     *         It is a base-58 encoded string that is uniquely generated for each transaction.
     * `optional field`
     */
    transactionId?: string;
    /**
     * The public key of the stake account
     * `optional field`
     */
    stakeAccount?: string;
    /**
     * When someone wants to delegate tokens in a stake account, the delegation command is pointed at the vote
     * account address of the validator to whom the token-holder wants to delegate.
     * `optional field`
     */
    voteAccount?: string;
    /**
     * account owner (public key), given authority to stake tokens
     * `optional field`
     */
    stakeAuthority?: string;
    /**
     * The number of results to return per page
     * `optional field`
     */
    limit?: number;
    /**
     * The page number to return
     * `optional field`
     */
    page?: number;
    /**
     * The pagination token to use to keep your position in the results
     * `optional field`
     */
    paginationToken?: string;
}
export declare class Stakeactionsv2Request extends BaseRequest<Stakeactionsv2RequestArgs, PaginatedResponse<Stakeactionsv2>> {
    readonly body: Stakeactionsv2RequestArgs;
    readonly method = "POST";
    readonly url = "/v0/staking/actions";
    constructor(body?: Stakeactionsv2RequestArgs);
}

import { BaseRequest } from "../BaseRequest.class";
import type { PaginatedResponse } from "../Response.interface";
export interface Tokencreationv2 {
    /**
     * Numeric identifier of a block describing the slot that the block was produced in
     */
    blockId: number;
    /**
     * HelloMoon unique identifier for specific instruction within a transaction
     */
    instructionId: string;
    /**
     * Unix epoch time (in seconds) of a block as calculated from validator votes.
     *     If you want to look at historical data, let's say 7 days in the past.
     *     1. _Change the **operator** to <_
     *     2. Get the **current epochtime** i.e, 1673831466 -> Jan 15, 2023
     *     3. Subtract the current epochtime from **( 86400 * 7 )**. Place the result of 1673831466 - ( 86400
     * * 7 ) = **1673226666** in the value input - this returns the epochtime time from 7 days ago
     */
    blockTime: number;
    /**
     * The token program used to mint, transfer and burn tokens
     */
    tokenprogram: string;
    /**
     * Number of decimals an SPL token's balance is measured in. For example USDC measures to 6 decimals so
     * 1 USDC is measured as 1000000
     */
    decimals: number;
    /**
     * account owner (public key), given authority to mint new tokens
     * `optional field`
     */
    mintAuthority: string;
    /**
     * Mint address per the SPL token program.
     *       > You can also visit https://www.hellomoon.io/id?search=token to search for mints using a user
     * interface.
     */
    mint: string;
    /**
     * account owner (public key), given authority to freeze token accounts - rendering an Account unusable
     * `optional field`
     */
    freezeAuthority: string;
}
export interface Tokencreationv2RequestArgs {
    blockTime?: {
        operator: "=" | "!=" | ">" | "<" | ">=" | "<=";
        /**
     * Unix epoch time (in seconds) of a block as calculated from validator votes.
     *     If you want to look at historical data, let's say 7 days in the past.
     *     1. _Change the **operator** to <_
     *     2. Get the **current epochtime** i.e, 1673831466 -> Jan 15, 2023
     *     3. Subtract the current epochtime from **( 86400 * 7 )**. Place the result of 1673831466 - ( 86400
     * * 7 ) = **1673226666** in the value input - this returns the epochtime time from 7 days ago
     */
        value: number;
    } | number;
    /**
     * account owner (public key), given authority to mint new tokens
     * `optional field`
     */
    mintAuthority?: string;
    /**
     * Mint address per the SPL token program.
     *       > You can also visit https://www.hellomoon.io/id?search=token to search for mints using a user
     * interface.
     */
    mint?: string;
    /**
     * account owner (public key), given authority to freeze token accounts - rendering an Account unusable
     * `optional field`
     */
    freezeAuthority?: string;
    /**
     * The number of results to return per page
     * `optional field`
     */
    limit?: number;
    /**
     * The page number to return
     * `optional field`
     */
    page?: number;
    /**
     * The pagination token to use to keep your position in the results
     * `optional field`
     */
    paginationToken?: string;
}
export declare class Tokencreationv2Request extends BaseRequest<Tokencreationv2RequestArgs, PaginatedResponse<Tokencreationv2>> {
    readonly body: Tokencreationv2RequestArgs;
    readonly method = "POST";
    readonly url = "/v0/token/creation";
    constructor(body?: Tokencreationv2RequestArgs);
}
